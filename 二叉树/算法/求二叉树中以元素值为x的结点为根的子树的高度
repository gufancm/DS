// 递归算法
// 求二叉树中以元素值为x的结点为根的子树的高度

typedef sturct BiTNode{
	ElemType data;
	BiTNode *lchild,*rchild;
}BiTNode,*BiTNode;

int getDepth(BiTree T)
{
	int l,r;
	if(T->data == x)
	{	l = getDepth(T-lchild);
		r = getDepth(T-rchild);
		return l > r ? l : r;
	}
	else
		return 0;
}
